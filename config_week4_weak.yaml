simulation:
  seed: 42
  num_agents: 1000
  dt: 0.001
  steps: 2000
  device: gpu

geometry:
  type: cylinder
  radius: 1.0
  length: 10.0

flow:
  type: poiseuille
  max_velocity: 1.0

output:
  base_dir: results/week4/weak
  save_every: 20

activation:
  near_wall_dist_frac: 0.10
  activation_rate: 0.5
  decay_rate: 0.5

adhesion:
  enabled: true
  act_threshold: 0.02
  stick_factor: 0.5

wall:
  restitution: 0.0