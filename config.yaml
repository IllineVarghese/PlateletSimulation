simulation:
  seed: 42
  num_agents: 1000
  dt: 0.001
  steps: 500
  device: gpu

geometry:
  type: cylinder
  radius: 1.0
  length: 10.0

flow:
  type: poiseuille
  max_velocity: 1.0

output:
  base_dir: results/week3
  save_every: 100
